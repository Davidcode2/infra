[defaults]
# Use the inventory file generated by Terraform
inventory = ./inventory/

# Disable checking SSH host keys, since these are brand new servers
host_key_checking = False

# Suppress warnings about disabling host key checking
deprecation_warnings = False
